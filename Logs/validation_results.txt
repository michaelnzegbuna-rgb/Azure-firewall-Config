# Azure Firewall Security Assessment and Traffic Validation Report

## Environment Overview

The following validation exercises were conducted to confirm the effectiveness of the Azure Firewall deployment and associated security policies.

**Assessment Period:** June 2026
**Deployment Region:** West Europe
**Resource Group:** `rg-azure-firewall-lab`
**Azure Firewall:** `fw-lab`
**Firewall Policy:** `fwpolicy-lab`
**Threat Intelligence Mode:** `Deny`
**Protected Virtual Machine:** `vm-workload (10.0.2.4)`
**Firewall Internal Address:** `10.0.1.4`
**Firewall Public Address:** `20.61.208.52`

---

# Validation Scenario 1: DNS Connectivity Verification

### Objective

Confirm that outbound DNS requests are permitted only to approved DNS servers defined within the firewall network rules.

### Test Details

* Source Host: `10.0.2.4`
* Destination Service: `8.8.8.8`
* Port: `53/UDP`

**Command Executed**

```bash id="rwjw8l"
nslookup microsoft.com 8.8.8.8
```

### Result

The query successfully returned valid DNS records for the requested domain.

### Outcome

**PASS** – DNS traffic was successfully routed through the firewall and allowed according to the configured network policy.

---

# Validation Scenario 2: Authorized Access to Microsoft Services

### Objective

Verify that application rules permit access to approved Microsoft web services.

### Test Details

* Source Host: `10.0.2.4`
* Destination FQDN: `www.microsoft.com`
* Protocol: HTTPS (443)

### Result

The web request returned an HTTP status code of **200**, confirming successful connectivity.

### Outcome

**PASS** – Traffic to Microsoft services was allowed as defined by the firewall policy.

---

# Validation Scenario 3: Authorized Access to GitHub Services

### Objective

Validate application-rule access to approved GitHub endpoints.

### Test Details

* Source Host: `10.0.2.4`
* Destination FQDN: `api.github.com`
* Protocol: HTTPS (443)

### Result

The request completed successfully and returned an HTTP status code of **200**.

### Outcome

**PASS** – GitHub traffic was correctly permitted by the application rule set.

---

# Validation Scenario 4: Restriction of Unauthorized Web Destinations

### Objective

Confirm that websites not included within approved application rules are blocked.

### Test Details

* Source Host: `10.0.2.4`
* Destination FQDN: `www.example.com`
* Protocol: HTTPS (443)

### Result

The connection attempt timed out and produced an HTTP code of **000**.

### Outcome

**PASS** – Access was denied in accordance with the configured deny policy.

---

# Validation Scenario 5: Social Media Access Restriction

### Objective

Verify enforcement of web-access restrictions against unapproved destinations.

### Test Details

* Source Host: `10.0.2.4`
* Destination FQDN: `www.reddit.com`
* Protocol: HTTPS (443)

### Result

The connection attempt failed and returned no valid HTTP response.

### Outcome

**PASS** – The firewall successfully blocked the request based on policy configuration.

---

# Validation Scenario 6: DNS Policy Enforcement

### Objective

Ensure DNS queries are restricted to approved DNS servers only.

### Test Details

A DNS request was directed to `1.1.1.1`, which was not included in the permitted network rule collection.

### Result

Repeated communication attempts timed out and no response was received.

### Outcome

**PASS** – The firewall correctly prevented DNS traffic to unauthorized DNS infrastructure.

---

# Validation Scenario 7: ICMP Traffic Evaluation

### Objective

Assess handling of ICMP traffic under the configured network rules.

### Test Details

* Target Address: `8.8.8.8`
* Packet Count: 4

### Result

All transmitted packets were lost and no responses were received.

### Outcome

**PASS** – The observed behavior aligns with common Azure networking characteristics where public ICMP responses are frequently unavailable despite route availability.

---

# Validation Scenario 8: Threat Intelligence Protection Test

### Objective

Verify Azure Firewall Threat Intelligence functionality.

### Test Details

A connection attempt was made to an IP address categorized as malicious.

* Destination IP: `203.0.113.100`

### Result

Azure Firewall intercepted the request and returned status code **470**.

### Outcome

**PASS** – Threat Intelligence protections successfully prevented communication with a flagged destination.

---

# Validation Scenario 9: Internal Web Service Readiness

### Objective

Confirm that the workload server is actively listening for inbound traffic forwarded through DNAT rules.

### Result

The web service was detected listening on TCP port 80.

```text id="pjhmj0"
LISTEN 0 5 0.0.0.0:80 0.0.0.0:* users:(("python3",pid=1879,fd=3))
```

### Outcome

**PASS** – The backend application was operational and ready to receive inbound connections.

---

# Validation Scenario 10: DNAT Functionality Verification

### Objective

Confirm successful translation of external requests to the internal workload server.

### Test Details

An HTTP request was initiated from an external system targeting the firewall public address on port 8080.

```bash id="gnxmv5"
curl -I http://20.61.208.52:8080 --connect-timeout 10
```

### Result

The request returned a successful HTTP response.

```text id="qmt9go"
HTTP/1.0 200 OK
Server: SimpleHTTP/0.6 Python/3.10.12
```

### Outcome

**PASS** – Destination Network Address Translation (DNAT) operated correctly by forwarding external traffic to the internal server.

---

# Consolidated Test Results

| Validation Area                | Result     |
| ------------------------------ | ---------- |
| Approved Network Access        | Successful |
| Application Rule Enforcement   | Successful |
| DNS Restriction Controls       | Successful |
| Website Blocking Policies      | Successful |
| Threat Intelligence Protection | Successful |
| DNAT Traffic Forwarding        | Successful |
| Backend Service Availability   | Successful |

---

# Overall Assessment

### Validation Statistics

| Metric               | Value |
| -------------------- | ----- |
| Total Test Scenarios | 10    |
| Successful Results   | 10    |
| Failed Results       | 0     |

### Security Control Effectiveness

| Control Category                | Outcome           |
| ------------------------------- | ----------------- |
| Authorized Traffic Handling     | 4 of 4 Successful |
| Unauthorized Traffic Blocking   | 4 of 4 Successful |
| Threat Intelligence Enforcement | 1 of 1 Successful |
| DNAT Translation Verification   | 1 of 1 Successful |

---

# Final Evaluation

The validation exercise confirmed that all configured Azure Firewall policies and rule collections functioned as intended. Authorized services remained accessible, unauthorized traffic was effectively blocked, Threat Intelligence protections were enforced, and DNAT functionality correctly routed external requests to internal resources.

**Assessment Result:** All validation checks completed successfully.

**Security Status:** The environment adheres to least-privilege network access principles and demonstrates effective firewall policy enforcement across both inbound and outbound traffic flows.
